---
version: 2
plan: DEMO-BSD
plan-permissions:
  - users: !include 'permissions/dev-users.yml'
    permissions:
      - view
      - edit
      - build
  - roles:
      - logged-in
    permissions:
      - view
